# tests 包的初始化文件
# 空文件使 tests/ 目录成为 Python 包，内含以下评测/演示脚本：
#   eval_routing.py    —— 路由精度评测（20 条用例，验收线 ≥80%）
#   eval_performance.py —— 性能压测（L1 工具层 <500ms / L2 端到端）
#   eval_robustness.py  —— 容错能力评测（10 条异常用例）
#   demo_thinking.py    —— Agent 思考过程演示（6 个典型场景）
