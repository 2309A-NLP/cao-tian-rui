# Agent22 后端依赖
# Python >= 3.10

# mem0 记忆管理（与原项目保持一致）
mem0ai>=0.1.50

# 向量库：Chroma 嵌入式（已验证，<200ms 检索）
chromadb>=0.5.0

# LLM / Embedding：硅基流动 OpenAI 兼容接口
openai>=1.30.0

# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# 工具
python-dotenv>=1.0.0
pydantic>=2.5.0
httpx>=0.27.0
